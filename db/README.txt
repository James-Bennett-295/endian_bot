Database set up instructions:

- Run master.sql with psql.
- Change password of "endian_bot" user from "DEFAULT_PASSWORD".
- Replace "PASSWORD", "HOST", and "PORT" in config.json "dbConnectionUrl" property.
